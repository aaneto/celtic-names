language: rust
matrix:
  fast_finish: true
  allow_failures: nightly
  include:
    - os: osx
      rust: stable
    
    - rust: stable
    - rust: beta
    - rust: nightly

    - os: windows
      rust: stable
  
script:
  - cargo build
  - cargo test